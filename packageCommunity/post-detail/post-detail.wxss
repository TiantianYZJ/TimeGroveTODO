@import "../../pages/todo/todo.wxss";

/* 卡片容器：包裹 post-card + 统计行 */
.post-card-wrapper {
  background: #fff;
  margin: 20rpx;
  border-radius: 32rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.06);
  overflow: hidden;
  padding: 0 0 30rpx;
}

.deleted-notice {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 200rpx 0;
  color: #999;
  font-size: 28rpx;
  gap: 20rpx;
}

.post-meta {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 16rpx;
  font-size: 24rpx;
  color: #999;
  padding: 0 30rpx;
}
.edited-badge {
  color: #00b26a;
  font-weight: 500;
}
.post-stats {
  display: flex;
  align-items: center;
  gap: 40rpx;
  padding: 16rpx 30rpx 0;
  border-top: 1rpx solid #f0f0f0;
}
.stat-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 28rpx;
  color: #999;
}
.stat-item.liked {
  color: #ff4757;
}
.more-btn {
  margin-left: auto;
}
.visitor-link {
  color: #00b26a;
}

/* 评论区 */
.comment-section {
  padding: 0 20rpx 64rpx 20rpx;
}
.comment-header {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  padding: 16rpx 0;
}
.no-comments {
  text-align: center;
  color: #999;
  font-size: 26rpx;
  padding: 60rpx 0;
}
.comment-item {
  margin-bottom: 20rpx;
  background: #fff;
  padding: 20rpx;
  border-radius: 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}
.comment-deleted {
  padding: 16rpx;
  background: #f9f9f9;
  border-radius: 12rpx;
  font-size: 24rpx;
  color: #999;
  text-align: center;
}
.comment-normal {
  display: flex;
  gap: 16rpx;
}
.comment-avatar {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  flex-shrink: 0;
}
.comment-body {
  flex: 1;
  min-width: 0;
}
.comment-header-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 4rpx;
}
.comment-nickname {
  font-size: 24rpx;
  font-weight: 600;
  color: #333;
  margin-right: 8rpx;
}
.comment-actions {
  display: flex;
  align-items: center;
  gap: 12rpx;
}
.action-btn {
  font-size: 22rpx;
  color: #00b26a;
  padding: 0;
  border-radius: 0;
}
.delete-btn {
  color: #ff4d4f;
  background: none;
}
.comment-content {
  font-size: 26rpx;
  color: #444;
  line-height: 1.6;
}
.comment-time {
  font-size: 20rpx;
  color: #bbb;
  margin-top: 4rpx;
  display: block;
}
.comment-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 8rpx;
}
.comment-like {
  display: flex;
  align-items: center;
  gap: 4rpx;
  font-size: 20rpx;
  color: #bbb;
  padding: 4rpx;
}
.comment-like.liked {
  color: #ff4d4f;
}
.like-count {
  font-size: 20rpx;
  color: inherit;
  min-width: 16rpx;
}
.replies-list {
  margin-top: 12rpx;
  padding: 12rpx;
  background: #f5f5f5;
  border-radius: 32rpx;
}

.reply-item {
  margin-bottom: 12rpx;
}

.reply-item:last-child {
  margin-bottom: 0;
}

.reply-normal {
  display: flex;
  gap: 10rpx;
}

.reply-avatar {
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  background: #f5f5f5;
  flex-shrink: 0;
}

.reply-body {
  flex: 1;
  min-width: 0;
}

.reply-meta {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 6rpx;
  margin-bottom: 4rpx;
}

.reply-nickname {
  font-size: 24rpx;
  font-weight: 600;
  color: #333;
  margin-right: 8rpx;
}

.reply-time {
  font-size: 20rpx;
  color: #bbb;
  display: block;
}

.reply-content {
  font-size: 26rpx;
  color: #444;
  line-height: 1.5;
}

.reply-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 6rpx;
}

.reply-actions {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.loading-comments {
  display: flex;
  justify-content: center;
  padding: 30rpx;
}

.bottom-image-bar {
  overflow-y: hidden;
  transition: all 0.3s ease;
  --td-upload-radius: 32rpx;
  --td-upload-add-bg-color: #f8f8f8;
  --td-grid-bg-color: #e3f5eb99;
  --td-grid-item-bg-color: #e3f5eb99;
}
.bar-collapse {
  max-height: 0;
  opacity: 0;
  padding: 0;
}
.bar-expand {
  max-height: 300rpx;
  opacity: 1;
  padding: 8rpx 0;
}

/* 底部输入栏 */
.bottom-input-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #e3f5eb99;
  backdrop-filter: blur(20rpx) saturate(180%);
  -webkit-backdrop-filter: blur(20rpx) saturate(180%);
  border-top: 1rpx solid rgba(0,0,0,0.05);
  z-index: 100;
  padding: 0rpx 18rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
}

.reply-target {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12rpx 16rpx;
  margin-bottom: 12rpx;
  background: #f5f5f5;
  border-radius: 32rpx;
  font-size: 26rpx;
  color: #666;
}

.reply-target-name {
  color: #00b26a;
  font-weight: 500;
}

.input-wrapper {
  margin-top: 8rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.comment-input {
  flex: 1;
  height: 72rpx;
  padding: 0 24rpx;
  background: #f5f5f5;
  border-radius: 32rpx;
  font-size: 30rpx;
}

.send-btn {
  width: 72rpx;
  height: 72rpx;
  padding: 0;
  background: #ddd;
  font-size: 28rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  flex-shrink: 0;
}

.send-btn.active {
  background: #00b26a;
}

.send-btn[disabled] {
  opacity: 0.4;
}

.send-btn::after {
  border: none;
}

.close-btn {
  text-align: center;
  padding: 20rpx 0 10rpx;
  font-size: 28rpx;
  color: #999;
}

.visitors-panel {
  padding: 24rpx;
  padding-bottom: calc(30rpx + env(safe-area-inset-bottom));
  max-height: 600rpx;
  overflow-y: auto;
}
.visitors-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 20rpx;
  text-align: center;
}
.no-visitors {
  text-align: center;
  color: #999;
  font-size: 26rpx;
  padding: 40rpx 0;
}
.visitor-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #f0f0f0;
}
.visitor-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
}
.visitor-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}
.visitor-nickname {
  font-size: 28rpx;
  color: #333;
}

.nickname-row {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8rpx;
}

.user-badge {
  font-size: 22rpx;
  padding: 2rpx 10rpx;
  border-radius: 32rpx;
  border: 2rpx solid;
  white-space: nowrap;
  line-height: 1.4;
}
.visitor-time {
  font-size: 22rpx;
  color: #999;
  display: block;
}

.tap-active {
  opacity: 0.6;
}

/* 评论@提及面板（固定定位，在输入栏上方，不遮键盘） */
.comment-at-suggestions {
  position: fixed;
  left: 12rpx;
  right: 12rpx;
  bottom: calc(120rpx + env(safe-area-inset-bottom));
  max-height: 40vh;
  background: rgba(255,255,255,0.96);
  backdrop-filter: blur(24rpx) saturate(180%);
  -webkit-backdrop-filter: blur(24rpx) saturate(180%);
  border-radius: 20rpx;
  box-shadow: 0 -4rpx 20rpx rgba(0,0,0,0.08);
  overflow: hidden;
  z-index: 9999;
}
.comment-at-suggestions .at-suggestions-header {
  display: flex; align-items: center;
  justify-content: space-between;
  padding: 16rpx 20rpx;
  border-bottom: 1rpx solid #f0f0f0;
}
.comment-at-suggestions .at-suggestions-title { font-size: 26rpx; font-weight: 600; color: #1a1a1a; }
.comment-at-suggestions .at-suggestions-close { padding: 6rpx; }
.comment-at-suggestions .at-suggestions-scroll { max-height: 32vh; overflow-y: auto; padding: 4rpx 0; }
.comment-at-suggestions .at-suggestions-empty { padding: 28rpx; text-align: center; font-size: 26rpx; color: #999; }
.comment-at-suggestions .at-user-item {
  display: flex; align-items: center; gap: 14rpx;
  padding: 14rpx 20rpx;
}
.comment-at-suggestions .at-user-item:active { background: #f5f5f5; }
.comment-at-suggestions .at-user-avatar {
  width: 48rpx; height: 48rpx; border-radius: 50%; flex-shrink: 0;
}
.comment-at-suggestions .at-user-name { font-size: 26rpx; color: #1a1a1a; }
.comment-at-suggestions .at-user-info {
  display: flex;
  flex-direction: column;
  gap: 2rpx;
  min-width: 0;
  flex: 1;
}
.comment-at-suggestions .at-user-id {
  font-size: 20rpx;
  color: #aaa;
}

/* @提及链接绿色 */
.comment-content-md .t-chat-markdown-link,
.reply-content-md .t-chat-markdown-link {
  color: var(--primary-color, #00b26a);
}
.comment-content-md,
.reply-content-md {
  display: block;
  margin: 8rpx 0;
  font-size: 26rpx;
  color: #444;
  line-height: 1.6;
}

/* ===== 投票卡片 ===== */
.poll-card {
  margin: 20rpx;
  padding: 24rpx;
  background: linear-gradient(135deg, #f0fdf6 0%, #e8faf0 100%);
  border-radius: 32rpx;
}
.poll-title {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #1a1a1a;
  margin-bottom: 16rpx;
  flex-wrap: wrap;
}
.poll-badge {
  font-size: 20rpx;
  color: #00b26a;
  background: #d4f5e3;
  padding: 2rpx 12rpx;
  border-radius: 32rpx;
}
.poll-badge-ended {
  font-size: 20rpx;
  color: #999;
  background: #f0f0f0;
  padding: 2rpx 12rpx;
  border-radius: 32rpx;
}
.poll-options {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}
.poll-option {
  background: #fff;
  border-radius: 32rpx;
  padding: 16rpx;
  border: 2rpx solid #e0e0e0;
  overflow: hidden;
  position: relative;
}
.poll-option.selected {
  border-color: #00b26a;
  background: #f0fdf6;
}
.poll-option.voted {
  border-color: #00b26a;
  background: #f0fdf6;
}
.poll-option.result {
  cursor: default;
}
.poll-option-content {
  display: flex;
  align-items: center;
  gap: 10rpx;
  position: relative;
  z-index: 1;
}
.poll-option-selector {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  border: 2rpx solid #ddd;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.poll-option-selector.radio.checked {
  background: #00b26a;
  border-color: #00b26a;
}
.poll-option-selector.checkbox {
  border-radius: 32rpx;
}
.poll-option-selector.checkbox.checked {
  background: #00b26a;
  border-color: #00b26a;
}
.poll-option-text {
  flex: 1;
  font-size: 28rpx;
  color: #333;
}
.poll-option-bar-wrap {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  right: 0;
  z-index: 0;
}
.poll-option-bar {
  height: 100%;
  background: rgba(0, 178, 106, 0.1);
  border-radius: 32rpx;
  transition: width 0.3s ease;
}
.poll-option-stats {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 22rpx;
  color: #999;
  flex-shrink: 0;
}
.poll-option-pct {
  font-weight: 600;
  color: #00b26a;
}
.poll-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 14rpx;
}
.poll-total {
  font-size: 24rpx;
  color: #999;
}
.poll-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
}
.poll-vote-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 56rpx;
  padding: 0 24rpx;
  font-size: 26rpx;
  color: #fff;
  background: #00b26a;
  border: none;
  border-radius: 32rpx;
  line-height: 1;
}
.poll-vote-btn::after {
  border: none;
}
.poll-revote {
  font-size: 24rpx;
  color: #00b26a;
  padding: 8rpx;
}
.poll-close-text {
  font-size: 24rpx;
  color: #999;
  padding: 8rpx;
}

/* ========== "其他"选项输入弹窗 ========== */
.other-popup-panel {
  padding: 40rpx 28rpx 40rpx;
  padding-bottom: calc(40rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.88);
  backdrop-filter: blur(40rpx) saturate(180%);
  -webkit-backdrop-filter: blur(40rpx) saturate(180%);
  border-radius: 36rpx 36rpx 0 0;
}
.other-popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 28rpx;
}
.other-popup-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1a1a1a;
  letter-spacing: 0.02em;
}
.other-popup-close {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
}
.other-popup-close:active {
  background: rgba(0, 0, 0, 0.05);
}
.other-popup-body {
  margin-bottom: 28rpx;
  position: relative;
}
.other-input {
  display: block;
  width: 100%;
  min-height: 180rpx;
  background: rgba(248, 249, 250, 0.9);
  border-radius: 20rpx;
  border: 2rpx solid rgba(0, 0, 0, 0.06);
  padding: 24rpx;
  font-size: 30rpx;
  line-height: 1.5;
  color: #1a1a1a;
  box-sizing: border-box;
  caret-color: #00b26a;
}
.other-count {
  position: absolute;
  right: 16rpx;
  bottom: 16rpx;
  font-size: 22rpx;
  color: #bbb;
  background: rgba(255,255,255,0.6);
  padding: 4rpx 12rpx;
  border-radius: 16rpx;
}
.other-popup-footer {
  display: flex;
  justify-content: center;
}
.other-confirm-btn {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  text-align: center;
  color: #fff;
  font-size: 30rpx;
  font-weight: 500;
  background: #00b26a;
  border-radius: 44rpx;
  border: none;
  transition: opacity 0.2s ease;
}
.other-confirm-btn:active {
  opacity: 0.8;
}
.other-confirm-btn::after {
  border: none;
}

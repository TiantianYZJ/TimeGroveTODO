.top {
  top: 0;
  left: 0;
  right: 0;
  z-index: 999;
  padding-bottom: 16rpx;
  position: fixed;
  background: #e3f5eb99;
  backdrop-filter: blur(20rpx) saturate(180%);
  -webkit-backdrop-filter: blur(20rpx) saturate(180%);
  overflow: visible;
}

/* 自定义公告栏样式 - 毛玻璃质感 */
.custom-notice {
  margin: 20rpx 20rpx 16rpx;
  border-radius: 32rpx;
  background: rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(24rpx) saturate(180%);
  -webkit-backdrop-filter: blur(24rpx) saturate(180%);
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  position: relative;
  overflow: hidden;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  animation: noticeSlideIn 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
}

/* 背景光晕装饰 */
.custom-notice::after {
  content: '';
  position: absolute;
  top: -30%;
  right: -20%;
  width: 50%;
  height: 120%;
  background: radial-gradient(circle, rgba(45, 212, 191, 0.07) 0%, transparent 70%);
  pointer-events: none;
}

/* 公告内容区 */
.notice-content {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx 16rpx 32rpx;
  gap: 16rpx;
  position: relative;
  z-index: 1;
}

/* 图标容器 - 动画 */
.notice-content > .t-icon {
  flex-shrink: 0;
  animation: noticeIconPulse 2.5s ease-in-out infinite;
}

@keyframes noticeIconPulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  10% {
    opacity: 0.7;
    transform: scale(1.15);
  }
  20% {
    opacity: 1;
    transform: scale(1);
  }
}

/* 文字区域 */
.notice-text-area {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.notice-text {
  font-size: 28rpx;
  color: #1e293b;
  font-weight: 500;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  letter-spacing: 0.5rpx;
  line-height: 1.4;
}

/* 右侧操作 */
.notice-action {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-shrink: 0;
}

/* NEW 角标 */
.notice-new-dot {
  width: 16rpx;
  height: 16rpx;
  background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
  border-radius: 50%;
  animation: noticeNewPulse 1.5s ease-in-out infinite;
}

@keyframes noticeNewPulse {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4);
  }
  50% {
    box-shadow: 0 0 0 8rpx rgba(239, 68, 68, 0);
  }
}

/* 按压反馈 */
.custom-notice:active {
  transform: scale(0.97);
  background: rgba(245, 245, 245, 0.85);
}

/* 入场弹性动画 */
@keyframes noticeSlideIn {
  0% {
    opacity: 0;
    transform: translateY(-20rpx) scale(0.95);
  }
  60% {
    opacity: 1;
    transform: translateY(4rpx) scale(1.02);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* ===========================
   天气卡片 - 动态背景 + 毛玻璃装饰
   =========================== */

.weather-card {
  position: relative;
  border-radius: 32rpx;
  padding: 28rpx 32rpx;
  margin: 20rpx 20rpx 16rpx;
  color: #ffffff;
  overflow: hidden;
  transition: all 0.3s ease;
  animation: weatherFloat 6s ease-in-out infinite;
}

/* 按压反馈 */
.weather-card:active {
  transform: scale(0.97);
}

/* 背景光晕装饰 */
.weather-bg-glow {
  position: absolute;
  top: -30%;
  right: -15%;
  width: 55%;
  height: 140%;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
  pointer-events: none;
  z-index: 0;
}

/* 浮动动画 */
@keyframes weatherFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-6rpx); }
}

/* 天气背景分类 */
.weather-default {
  background: linear-gradient(135deg, #3ddaa0 0%, #12b086 100%);
  box-shadow: 0 8rpx 24rpx rgba(0, 178, 106, 0.25);
}

.weather-sunny {
  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
  box-shadow: 0 8rpx 24rpx rgba(251, 191, 36, 0.3);
}

.weather-cloudy {
  background: linear-gradient(135deg, #93a5cf 0%, #6b7fa8 100%);
  box-shadow: 0 8rpx 24rpx rgba(107, 127, 168, 0.25);
}

.weather-overcast {
  background: linear-gradient(135deg, #7a8a9e 0%, #5a6a7e 100%);
  box-shadow: 0 8rpx 24rpx rgba(90, 106, 126, 0.25);
}

.weather-rainy {
  background: linear-gradient(135deg, #4a90d9 0%, #357abd 100%);
  box-shadow: 0 8rpx 24rpx rgba(74, 144, 217, 0.25);
}

.weather-snowy {
  background: linear-gradient(135deg, #a0b4cc 0%, #7a8fa8 100%);
  box-shadow: 0 8rpx 24rpx rgba(122, 143, 168, 0.25);
}

.weather-foggy {
  background: linear-gradient(135deg, #b0b8c4 0%, #8a94a0 100%);
  box-shadow: 0 8rpx 24rpx rgba(138, 148, 160, 0.2);
}

.weather-windy {
  background: linear-gradient(135deg, #5fa8a0 0%, #3d8b83 100%);
  box-shadow: 0 8rpx 24rpx rgba(63, 139, 131, 0.25);
}

/* 内容布局 */
.weather-card-content {
  display: flex;
  align-items: center;
  position: relative;
  z-index: 1;
}

/* 天气图标区 */
.weather-icon-area {
  flex-shrink: 0;
  margin-right: 24rpx;
  width: 88rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 24rpx;
  backdrop-filter: blur(8rpx);
  -webkit-backdrop-filter: blur(8rpx);
}

.weather-icon-area > .t-icon {
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 中间信息区 */
.weather-info-area {
  flex: 1;
  display: flex;
  align-items: baseline;
  gap: 16rpx;
  min-width: 0;
}

.temperature {
  font-size: 64rpx;
  font-weight: 700;
  line-height: 1;
  letter-spacing: -2rpx;
}

.temp-unit {
  font-size: 32rpx;
  font-weight: 400;
  opacity: 0.8;
}

.weather-text {
  font-size: 28rpx;
  font-weight: 500;
  opacity: 0.9;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 右侧信息区 */
.weather-meta-area {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  flex-shrink: 0;
}

.weather-meta-area .location {
  display: flex;
  align-items: center;
  gap: 6rpx;
  margin-bottom: 8rpx;
}

.city {
  font-size: 26rpx;
  font-weight: 500;
  opacity: 0.95;
}

.update-time {
  font-size: 22rpx;
  opacity: 0.65;
}

/* 新增头部样式 */
.header {
  padding: 0 30rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 88rpx;
}

.title {
  font-size: 50rpx;
  font-weight: 600;
  color: #2d3436;
  flex: 1;
}

.action-group {
  position: absolute;
  display: flex;
  align-items: center;
}

.container {
  padding-top: 265rpx;
  padding-bottom: 400rpx;
  overflow-y: auto;
}

/* 新增搜索框样式 */
.search-container {
  padding-top: 10rpx;
  padding-left: 20rpx;
  padding-right: 20rpx;
}

.search-box:active {
  background: #e0e0e0;
}

.search-box {
  display: flex;
  align-items: center;
  height: 72rpx;
  background: #fff;
  border-radius: 32rpx;
  padding: 0 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.06);
}

.search-icon {
  width: 36rpx;
  height: 36rpx;
  margin-right: 16rpx;
}

/* 搜索图标微动效 */
.search-box:active .search-icon {
  transform: rotate(10deg) scale(0.9);
  transition: transform 0.15s ease;
}

.search-text {
  flex: 1;
  font-size: 28rpx;
  color: #666;
}

.placeholder-style {
  color: #999;
  font-size: 28rpx;
}

/* 卡片式列表项 */
.todo-item {
  margin: 12rpx 20rpx;
  background: #ffffff;
  border-radius: 32rpx;
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.08);
  transition:
    transform 0.3s ease,
    box-shadow 0.3s ease,
    background 0.5s ease,
    background-color 0.5s ease;
  position: relative;
  display: flex;
}

/* 收藏角标样式 */
.star-badge {
  position: absolute;
  top: 0;
  left: 0;
  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
  color: #ffffff;
  font-size: 22rpx;
  padding: 8rpx;
  border-radius: 0 0 30rpx 0;
  font-weight: 600;
  z-index: 10;
  animation: badgeBounce 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

@keyframes badgeBounce {
  0% {
    transform: scale(0) rotate(-15deg);
    opacity: 0;
  }
  50% {
    transform: scale(1.1) rotate(5deg);
  }
  100% {
    transform: scale(1) rotate(0deg);
    opacity: 1;
  }
}

/* 卡片进入动画 */
@keyframes slideInUp {
  0% {
    transform: translateY(40rpx) scale(0.95);
    opacity: 0;
  }
  60% {
    transform: translateY(-8rpx) scale(1.02);
    opacity: 1;
  }
  100% {
    transform: translateY(0) scale(1);
    opacity: 1;
  }
}

/* 卡片移出动画 */
@keyframes slideOutRight {
  0% {
    transform: translateX(0) scale(1);
    opacity: 1;
  }
  100% {
    transform: translateX(120%) scale(0.8);
    opacity: 0;
  }
}

/* 卡片点击波纹动画 */
@keyframes cardPulse {
  0% {
    box-shadow:
      0 4rpx 8rpx rgba(0,0,0,0.1),
      0 0 0 0 rgba(45, 212, 191, 0);
  }
  50% {
    box-shadow:
      0 4rpx 8rpx rgba(0,0,0,0.1),
      0 0 0 12rpx rgba(45, 212, 191, 0.2);
  }
  100% {
    box-shadow:
      0 4rpx 8rpx rgba(0,0,0,0.1),
      0 0 0 24rpx rgba(45, 212, 191, 0);
  }
}

/* 完成态切换动画 */
@keyframes completePulse {
  0% {
    background: linear-gradient(135deg, #ffffff 0%, #f8fdf9 100%);
    transform: scale(1);
  }
  30% {
    background: linear-gradient(135deg, #ecfdf5 0%, #90e0b7 100%);
    transform: scale(0.99);
  }
  70% {
    transform: scale(1.01);
  }
  100% {
    background: linear-gradient(135deg, #ecfdf5 0%, #90e0b7 100%);
    transform: scale(1);
  }
}

/* 完成态首次切换动画类 */
.todo-item.first-complete {
  animation: completePulse 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
}

/* 添加卡片进入动画类 */
.todo-item.add-animation {
  animation: slideInUp 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
}

/* 添加卡片移出动画类 */
.todo-item.remove-animation {
  animation: slideOutRight 0.4s cubic-bezier(0.55, 0.085, 0.68, 0.53) forwards;
}

/* 卡片点击效果 */
.todo-item:active {
  animation: cardPulse 0.3s ease;
}

.todo-item .delete-icon:active {
  opacity: 0.6;
}

.todo-content {
  flex: 1;
  margin: 0 0;
}

/* t-cell 的完成态样式（内部文字颜色等） */
.todo-item.completed .t-cell {
  background: transparent !important;
}

/* swipe-cell 的完成态样式（背景色）- 无动画，只有样式 */
.todo-item.completed {
  background: linear-gradient(135deg, #ecfdf5 0%, #90e0b7 100%) !important;
  box-shadow: 0 4rpx 16rpx rgba(76,175,80,0.15);
}

/* 完成态文字样式 */
.todo-item.completed .t-cell__title {
  color: #00b26a !important;
  text-decoration: line-through;
}

.todo-item.completed .t-cell__description {
  color: #6b7280 !important;
  opacity: 0.7;
}

.todo-item.completed .t-cell__note {
  color: #9ca3af !important;
}

.completed-decorate {
  position: absolute;
  right: 20rpx;
  top: 50%;
  transform: translateY(-50%);
  color: #4CAF50;
  text-shadow: 0 2rpx 4rpx rgba(0,0,0,0.1);
}

@keyframes ripple {
  0% {
    transform: scale(0);
    opacity: 1;
    box-shadow: 0 0 0 6rpx rgba(76,175,80,0.2);
  }
  50% {
    opacity: 0.6;
    box-shadow: 
      0 0 0 12rpx rgba(76,175,80,0.15),
      0 -8rpx 20rpx rgba(76,175,80,0.1);
  }
  100% {
    transform: scale(2.5);
    opacity: 0;
    box-shadow: 
      0 0 0 24rpx rgba(76,175,80,0),
      0 -16rpx 40rpx rgba(76,175,80,0);
  }
}

/* 新增动画效果 */
@keyframes slideIn {
  from { transform: translateY(20rpx); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

@keyframes fadeOut {
  from { transform: scale(1); opacity: 1; }
  to { transform: scale(0.8); opacity: 0; }
}

.add-animation {
  animation: slideIn 0.3s ease forwards;
}

.remove-animation {
  animation: fadeOut 0.3s ease forwards;
}

.todo-item {
  overflow: hidden;
  position: relative;
}

.swipe-content {
  width: 100%;
  transition: transform 0.3s ease;
  display: flex;
  align-items: center;
}

/* 拖动样式 */
.todo-item.dragging {
  z-index: 999;
  transition:
    transform 0.2s cubic-bezier(0.4, 0, 0.2, 1),
    box-shadow 0.2s ease,
    background 0.2s ease;
  transform: scale(1.03) translateY(-4rpx);
  box-shadow:
    0 20rpx 60rpx rgba(45, 212, 191, 0.3),
    0 8rpx 24rpx rgba(0, 0, 0, 0.15);
}

.todo-item.dragging .t-cell {
  transition: background-color 0.2s ease;
  background: linear-gradient(135deg, #f0fdf9 0%, #ecfdf5 100%) !important;
}

/* 在header样式后添加 */
.clear-btn {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: #F3FBF6;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.clear-btn:active {
  background: #f5f5f5;
  transform: scale(0.95);
}

.btn-wrapper {
  height: 100%;
}

.btn {
  display: inline-flex;
  justify-content: center;
  align-items: center;
  width: 110rpx;
  height: 100%;
  color: white;
}

.delete-btn {
  background-color: #e34d59;
}

.edit-btn {
  background-color: #ed7b2f;
}

/* 强制cell填满容器 */
.t-cell {
  min-height: 100rpx !important;
  padding: 20rpx 30rpx !important;
  width: 720rpx !important;
  box-sizing: border-box;
  transition: 
    transform 0.4s cubic-bezier(0.23, 1, 0.32, 1),
    box-shadow 0.4s ease,
    background-color 0.3s ease;
}

/* 标题样式调整 */
.t-cell__title {
  flex: 1;
  min-width: 465rpx;
  max-width: 465rpx;
  margin-right: 10rpx !important;
  transform: translateY(10%);
  font-size: 34rpx !important;  /* 调大字号 */
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;  /* 新增标题截断 */
}

/* 备注描述样式调整 */
.t-cell__description {
  flex: 1;
  max-width: 420rpx;  /* 增大最大宽度 */
  margin-right: 10rpx !important;
  transform: translateY(-25%);
  font-size: 28rpx !important;  /* 调小字号 */
  display: -webkit-box;
  -webkit-box-orient: vertical;
}

/* 日期样式调整 */
.t-cell__note {
  flex-shrink: 0;
  font-size: 24rpx !important;  /* 调小字号 */
  color: #666 !important;
  margin-top: 8rpx !important;  /* 与备注保持间距 */
}

/* 右侧复选框定位调整 */
.t-cell__right-icon {
  position: absolute;
  right: 20rpx;
  top: 50%;
  transform: translateY(-50%);
}

.inline {
  display: inline;
}

/* 广告容器样式 */
.ad-container {
  margin: 20rpx;
  background: #ffffff;
  border-radius: 32rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 8rpx rgba(0,0,0,0.1);
}

.ad-container ad-custom {
  width: 100%;
  display: block;
}

/* ===========================
   拖拽功能样式 - 占位符 + 平滑动画
   =========================== */

/* 列表滚动容器 */
.todo-list-scroll {
  width: 100%;
}

/* 拖拽提示 */
.drag-hint {
  position: fixed;
  top: 280rpx;
  left: 50%;
  transform: translateX(-50%);
  z-index: 1001;
  background: rgba(0, 178, 106, 0.9);
  padding: 16rpx 40rpx;
  border-radius: 40rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 178, 106, 0.3);
  animation: hintSlideDown 0.3s ease;
}

@keyframes hintSlideDown {
  from {
    transform: translateX(-50%) translateY(-20rpx);
    opacity: 0;
  }
  to {
    transform: translateX(-50%) translateY(0);
    opacity: 1;
  }
}

.drag-hint-text {
  color: #ffffff;
  font-size: 28rpx;
  font-weight: 500;
}

/* 占位符样式 */
.todo-item.placeholder {
  border: 3rpx dashed #00b26a;
  background: rgba(0, 178, 106, 0.08);
  animation: placeholderPulse 1.2s ease-in-out infinite;
  min-height: 120rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

@keyframes placeholderPulse {
  0%, 100% {
    background: rgba(0, 178, 106, 0.05);
    border-color: rgba(0, 178, 106, 0.4);
  }
  50% {
    background: rgba(0, 178, 106, 0.15);
    border-color: rgba(0, 178, 106, 0.8);
  }
}

.placeholder-content {
  display: flex;
  align-items: center;
  gap: 16rpx;
  color: #00b26a;
}

.placeholder-text {
  font-size: 28rpx;
  color: #00b26a;
  font-weight: 500;
}

/* 浮动拖拽项 */
.dragging-float {
  position: fixed;
  z-index: 1000;
  pointer-events: none;
  width: calc(100vw - 40rpx);
  transition: none;
}

.dragging-content {
  background: #ffffff;
  border-radius: 32rpx;
  box-shadow:
    0 20rpx 60rpx rgba(0, 0, 0, 0.25),
    0 8rpx 24rpx rgba(45, 212, 191, 0.2);
  transform: scale(1.03);
  animation: dragLift 0.2s ease;
  position: relative;
  overflow: hidden;
}

@keyframes dragLift {
  from {
    transform: scale(1);
    box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.1);
  }
  to {
    transform: scale(1.03);
    box-shadow:
      0 20rpx 60rpx rgba(0, 0, 0, 0.25),
      0 8rpx 24rpx rgba(45, 212, 191, 0.2);
  }
}

/* 浮动项中的收藏角标 */
.dragging-content .star-badge {
  position: absolute;
  top: 0;
  left: 0;
  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
  color: #ffffff;
  font-size: 22rpx;
  padding: 8rpx;
  border-radius: 0 0 30rpx 0;
  font-weight: 600;
  z-index: 10;
}

/* 禁用列表项的过渡动画，避免拖拽时的闪烁 */
.todo-item {
  transition:
    transform 0.3s cubic-bezier(0.23, 1, 0.32, 1),
    box-shadow 0.3s ease,
    background 0.5s ease;
}

/* 拖拽时禁用内部点击 */
.todo-item.dragging-disabled {
  pointer-events: none;
}

.tab-container {
  display: flex;
  margin: 20rpx;
  background: #fff;
  border-radius: 32rpx;
  padding: 8rpx;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.06);
}

.tab-item {
  flex: 1;
  text-align: center;
  padding: 12rpx 0;
  font-size: 32rpx;
  color: #666;
  border-radius: 32rpx;
  transition: all 0.3s ease;
}

.tab-item.active {
  background: linear-gradient(135deg, #00b26a 0%, #3ddaa0 100%);
  color: #fff;
  font-weight: 500;
}

.tab-dropdown-wrapper {
  flex: 1;
  position: relative;
}

.dropdown-trigger {
  display: flex;
  align-items: center;
  justify-content: center;
}

.dropdown-menu {
  position: fixed;
  left: 20rpx;
  right: 20rpx;
  margin-top: 8rpx;
  background: rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(24rpx) saturate(180%);
  -webkit-backdrop-filter: blur(24rpx) saturate(180%);
  border-radius: 32rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  z-index: 1001;
  max-height: 60vh;
  overflow-y: auto;
}

.dropdown-item {
  display: flex;
  align-items: center;
  padding: 18rpx 32rpx;
  font-size: 32rpx;
  color: #333;
  transition: background 0.2s;
}

.dropdown-item:first-child {
  border-radius: 32rpx 32rpx 0 0;
}

.dropdown-item:last-child {
  border-radius: 0 0 32rpx 32rpx;
}

.dropdown-item:active {
  background: #f5f5f5;
}

.dropdown-item.selected {
  color: #00b26a;
  font-weight: 500;
  background: #e8f8f0;
}

.dropdown-divider {
  height: 1rpx;
  background: #eee;
  margin: 8rpx 0;
}

.dropdown-group-label {
  padding: 12rpx 32rpx 4rpx;
  font-size: 32rpx;
  color: #999;
  font-weight: 500;
}

.tag-color-dot {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  margin-right: 16rpx;
  flex-shrink: 0;
}

.tag-color-dot.priority-dot.p1 { background: #e34d59; }
.tag-color-dot.priority-dot.p2 { background: #2196F3; }
.tag-color-dot.priority-dot.p3 { background: #ff9800; }
.tag-color-dot.priority-dot.p4 { background: #999; }

.dropdown-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 999;
  background: rgba(0, 0, 0, 0.3);
}

.todo-item-wrapper {
  position: relative;
  margin-bottom: 0rpx;
  border-radius: 32rpx;
  overflow: hidden;
  transition: all 0.3s ease;
}

.combo-list {
  margin: 0 20rpx;
}

.combo-card {
  display: flex;
  align-items: center;
  background: #fff;
  border-radius: 32rpx;
  padding: 24rpx 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.06);
  transition: all 0.3s ease;
}

.combo-card:active {
  transform: scale(0.98);
}

.combo-icon {
  width: 88rpx;
  height: 88rpx;
  border-radius: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 24rpx;
}

.combo-icon.add-icon {
  background: #f0fdf4;
  border: 2rpx dashed #00b26a;
}

.combo-icon.add-icon.shared {
  border: 2rpx solid #10b981;
}

.combo-info {
  flex: 1;
}

.combo-name {
  font-size: 32rpx;
  font-weight: 500;
  color: #333;
  display: block;
}

.combo-count {
  font-size: 26rpx;
  color: #999;
}

.combo-card.shared {
  border: 4rpx solid #81c784;
}

.role-badge {
  font-size: 22rpx;
  padding: 6rpx 16rpx;
  background: linear-gradient(135deg, #66bb6a 0%, #43a047 100%);
  color: #fff;
  border-radius: 32rpx;
  margin-right: 16rpx;
}

/* ===========================
   登录引导弹窗样式
   =========================== */

.login-popup {
  width: 580rpx;
  padding: 48rpx 40rpx 40rpx;
  background: rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(24rpx) saturate(180%);
  -webkit-backdrop-filter: blur(24rpx) saturate(180%);
  border-radius: 24rpx;
  text-align: center;
  border: 1rpx solid rgba(255, 255, 255, 0.3);
}

.login-popup-header {
  margin-bottom: 24rpx;
}

.login-popup-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 16rpx;
}

.login-popup-desc {
  font-size: 28rpx;
  color: #666;
  line-height: 1.6;
  margin-bottom: 40rpx;
}

.login-popup-actions {
  display: flex;
  gap: 16rpx;
}

/* ===========================
   组合邀请弹窗样式
   =========================== */

.invite-popup {
  width: 620rpx;
  background: rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(24rpx) saturate(180%);
  -webkit-backdrop-filter: blur(24rpx) saturate(180%);
  border-radius: 32rpx;
  position: relative;
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  overflow: hidden;
}

.invite-popup-header {
  padding: 32rpx;
  text-align: center;
}

.invite-popup-header-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #666;
}

.invite-popup-content {
  padding: 40rpx;
  text-align: center;
}

.invite-combo-icon {
  width: 120rpx;
  height: 120rpx;
  border-radius: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 24rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15);
}

.invite-combo-name {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 16rpx;
}

.invite-combo-info {
  font-size: 28rpx;
  color: #666;
  line-height: 1.6;
  margin-bottom: 20rpx;
  padding: 16rpx 24rpx;
  background: #f8f9fa;
  border-radius: 16rpx;
}

.invite-combo-meta {
  display: flex;
  justify-content: center;
  gap: 32rpx;
}

.invite-combo-meta .meta-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 26rpx;
  color: #999;
}

.invite-popup-actions {
  padding: 0 40rpx 40rpx;
}

.invite-popup-close {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #eee;
  border-radius: 50%;
}

.approval-popup {
  width: 700rpx;
  max-height: 80vh;
  background: rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(24rpx) saturate(180%);
  -webkit-backdrop-filter: blur(24rpx) saturate(180%);
  border-radius: 32rpx;
  position: relative;
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  overflow: hidden;
}

.approval-popup-header {
  display: flex;
  align-items: baseline;
  padding: 24rpx;
  border-bottom: 1rpx solid #e8f8f0;
}

.approval-popup-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.approval-popup-count {
  font-size: 24rpx;
  color: #999;
  margin-left: 24rpx;
}

.approval-popup-close {
  position: absolute;
  right: 32rpx;
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #eee;
  border-radius: 50%;
}

.approval-scroll {
  max-height: 80vh;
  padding: 8rpx;
}

.approval-group {
  margin-bottom: 24rpx;
  background: #f8f9fa;
  border-radius: 32rpx;
  overflow: hidden;
  margin: 12rpx;
  width: 95%;
}

.approval-group:last-child {
  margin-bottom: 0;
}

.approval-group-header {
  display: flex;
  align-items: center;
  padding: 20rpx 24rpx;
  background: #eee;
}

.group-combo-icon {
  width: 64rpx;
  height: 64rpx;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 16rpx;
}

.group-combo-info {
  flex: 1;
}

.group-combo-name {
  font-size: 28rpx;
  font-weight: 500;
  color: #333;
  display: block;
}

.group-combo-id {
  font-size: 22rpx;
  color: #999;
}

.group-request-count {
  font-size: 24rpx;
  color: #00b26a;
  background: rgba(0, 178, 106, 0.1);
  padding: 6rpx 16rpx;
  border-radius: 16rpx;
}

.approval-group .approval-list {
  padding: 16rpx;
}

.approval-group .approval-item {
  display: flex;
  background: #fff;
  border-radius: 32rpx;
  padding: 16rpx;
  margin-bottom: 12rpx;
  border-bottom: none;
}

.approval-group .approval-item:last-child {
  margin-bottom: 0;
}

.approval-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  margin-right: 16rpx;
}

.approval-user-info {
  flex: 1;
}

.approval-user-name {
  font-size: 28rpx;
  color: #333;
  display: block;
}

.approval-time {
  font-size: 22rpx;
  color: #999;
  margin-top: 4rpx;
  display: block;
}

.approval-actions {
  display: flex;
  gap: 12rpx;
  align-items: center;
  justify-content: center;
}

.action-btn {
  padding: 8rpx 24rpx;
  border-radius: 32rpx;
  font-size: 24rpx;
}

.action-btn.approve {
  background: #08bd8f;
  color: #fff;
}

.action-btn.reject {
  background: #f5f5f5;
  color: #d90000;
}
/* ===========================
   优先级左侧弧度色条（圆角向右包裹）
   =========================== */
.priority-bar {
  position: absolute;
  left: 0;
  top: 16rpx;
  width: 8rpx;
  height: calc(100% - 32rpx);
  border-radius: 0 40rpx 40rpx 0;
  z-index: 1;
  pointer-events: none;
}
.priority-bar[data-priority="p1"] {
  background: #e34d4d;
}
.priority-bar[data-priority="p2"] {
  background: #2196F3;
}
.priority-bar[data-priority="p3"] {
  background: #FF9800;
}
.priority-bar[data-priority="p4"],
.priority-bar:not([data-priority]) {
  background: transparent;
}

/* ===========================
   卡片长按操作 - 原位 cell + 贴边菜单
   =========================== */

/* 遮罩层：全屏半透明黑 */
.card-action-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 12000;
  background: rgba(0, 0, 0, 0.55);
  opacity: 0;
  transition: opacity 0.25s ease;
}

.card-action-overlay.overlay-show {
  opacity: 1;
}

/* 原位克隆 cell */
.card-action-cell-clone {
  position: fixed;
  z-index: 12001;
  pointer-events: none;
  overflow: hidden;
}

/* ===========================
   操作菜单 - 毛玻璃贴边弹出
   =========================== */

.card-action-menu {
  position: fixed;
  z-index: 12001;
  display: flex;
  justify-content: space-around;
  align-items: center;
  background: rgba(255, 255, 255, 0.18);
  backdrop-filter: blur(24rpx) saturate(180%);
  -webkit-backdrop-filter: blur(24rpx) saturate(180%);
  border-radius: 32rpx;
  padding: 16rpx 12rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.25);
  pointer-events: none;
  box-sizing: border-box;
}

/* 上半屏 cell → 往下弹出 */
.card-action-menu.down {
  opacity: 0;
  transform: translateY(-20rpx);
  transition: opacity 0.3s ease, transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.card-action-menu.down.show {
  opacity: 1;
  transform: translateY(0);
  pointer-events: auto;
}

/* 下半屏 cell → 往上弹出 */
.card-action-menu.up {
  opacity: 0;
  transform: translateY(20rpx);
  transition: opacity 0.3s ease, transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.card-action-menu.up.show {
  opacity: 1;
  transform: translateY(0);
  pointer-events: auto;
}

.menu-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 16rpx 32rpx;
  border-radius: 20rpx;
  transition: background 0.2s ease;
}

.menu-btn:active {
  background: rgba(255, 255, 255, 0.2);
}

.menu-label {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.95);
  font-weight: 500;
}

/* ===========================
   语音识别遮罩动画
   =========================== */

.voice-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 15000;
  pointer-events: auto;
}

.voice-overlay-inner {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.92);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: opacity 0.25s ease;
  -webkit-transition: opacity 0.25s ease;
}

.voice-overlay-inner.show {
  opacity: 1;
}

/* 中心文字区域 */
.voice-center {
  position: relative;
  z-index: 2;
  text-align: center;
  padding: 0 60rpx;
  width: 100%;
  box-sizing: border-box;
}

.voice-hint {
  display: block;
  font-size: 36rpx;
  color: #ffffff;
  line-height: 1.6;
  text-shadow: 0 4rpx 20rpx rgba(0, 214, 143, 0.3);
  word-break: break-all;
}

.voice-hint.placeholder {
  font-size: 34rpx;
  opacity: 0.6;
  font-weight: 400;
}

.voice-hint-tip {
  display: block;
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.4);
  margin-top: 24rpx;
}

/* 波浪容器 */
.voice-wave {
  position: absolute;
  bottom: 160rpx;
  left: 10%;
  right: 10%;
  height: 80rpx;
  overflow: hidden;
  z-index: 1;
}

.voice-wave-bar {
  position: absolute;
  bottom: 0;
  width: 6rpx;
  border-radius: 3rpx;
  background: linear-gradient(to top, #00d68f, rgba(0, 214, 143, 0.1));
  animation: voiceWaveAnim 1.2s ease-in-out infinite;
  transform-origin: bottom;
}

@keyframes voiceWaveAnim {
  0%, 100% {
    height: 12rpx;
    opacity: 0.3;
  }
  50% {
    height: 72rpx;
    opacity: 1;
  }
}

/* 底部光晕 */
.voice-glow {
  position: absolute;
  bottom: 100rpx;
  left: 5%;
  right: 5%;
  height: 160rpx;
  background: radial-gradient(ellipse at center, rgba(0, 214, 143, 0.15) 0%, transparent 70%);
  pointer-events: none;
  z-index: 0;
}

/* ===========================
   复制弹窗
   =========================== */

.copy-popup {
  width: 580rpx;
  background: #ffffff;
  border-radius: 32rpx;
  padding: 48rpx 40rpx;
  box-sizing: border-box;
}

.copy-popup-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #333;
  text-align: center;
  margin-bottom: 32rpx;
}

.copy-popup-input {
  width: 100%;
  height: 88rpx;
  background: #f8f9fa;
  border-radius: 16rpx;
  padding: 0 24rpx;
  font-size: 30rpx;
  color: #333;
  box-sizing: border-box;
  margin-bottom: 24rpx;
}

.copy-popup-options {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.copy-popup-checkbox-label {
  display: flex;
  align-items: center;
  gap: 16rpx;
  font-size: 28rpx;
  color: #666;
  line-height: 1.4;
}

.pseudo-checkbox {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  border: 3rpx solid #ccc;
  background: #fff;
  transition: all 0.2s ease;
}

.pseudo-checkbox.checked {
  background: #00b26a;
  border-color: #00b26a;
}

.copy-popup-buttons {
  display: flex;
  gap: 24rpx;
}

.copy-popup-btn {
  flex: 1;
  height: 88rpx;
  border-radius: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30rpx;
  font-weight: 500;
}

.copy-popup-btn-cancel {
  background: #f0f0f0;
  color: #666;
}

.copy-popup-btn-cancel:active {
  background: #e0e0e0;
}

.copy-popup-btn-confirm {
  background: linear-gradient(135deg, #3ddaa0 0%, #12b086 100%);
  color: #ffffff;
}

.copy-popup-btn-confirm:active {
  opacity: 0.85;
}

/* ===========================
   分享弹窗
   =========================== */

.share-popup {
  padding: 48rpx 40rpx 60rpx;
  text-align: center;
  background: #ffffff;
  border-radius: 32rpx;
}

.share-popup-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 16rpx;
}

.share-popup-desc {
  font-size: 28rpx;
  color: #666;
  margin-bottom: 40rpx;
}

.share-popup-btn {
  width: 400rpx;
  height: 96rpx;
  line-height: 96rpx;
  background: linear-gradient(135deg, #3ddaa0 0%, #12b086 100%);
  border-radius: 48rpx;
  font-size: 32rpx;
  font-weight: 500;
  color: #ffffff;
  text-align: center;
  margin: 0 auto 24rpx;
  border: none;
}

.share-popup-btn::after {
  border: none;
}

.share-popup-btn:active {
  opacity: 0.85;
}

.share-popup-cancel {
  font-size: 28rpx;
  color: #999;
  text-align: center;
  padding: 16rpx;
}
